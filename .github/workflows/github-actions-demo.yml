name: GitHub Actions Demo
on: [push]
jobs:
 Explore-GitHub-Actions:
   runs-on: ubuntu-latest
   steps:
      - name: checkout repo
        uses: actions/checkout
      - run: npm ci 
      - run: npm test
      - run: echo "🎉 Good job!"